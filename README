== Welcome to Finanze

mysqldump -uroot -pPASSWORD --no-create-db=TRUE -t --compact finanze_development > dbdump.sql

mysql -uroot -psatana --database=finanze_production < dbdump.sql

export RAILS_ENV=production; rake db:migrate; export RAILS_ENV=development;
